# Python is ONLY used for HTTP data ingestion (data_ingest/alexj/*.py).
# Every big-data step — ETL, geocoding, scoring, analytics, Kafka
# producing/consuming — runs on Scala/Spark and has no Python footprint.
# The web dashboard is a static bundle that reads parquet directly with
# hyparquet in the browser, so no backend server / no Python at serve time.
requests>=2.31
